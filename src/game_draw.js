function drawBoard(){
	llog('drawboard','',0);
}
function drawBoardHighlights(){
	llog('drawBoardHighlights','',0);
}
function drawEntities(){
	llog('drawEntities','',0);
}
function drawMenu(){
	llog('drawMenu','',0);
}
function drawTileHighlights(){
	llog('drawTileHighlights','',0);
}
function drawTile(){
	llog('drawTile','',0);
}
